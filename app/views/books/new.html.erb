<%= form_with model: @book do |f| %>
  <div class="field">
    <h4>title</h4>
    <%= f.text_field :title %>
  </div>
  <div class="field">
   <h4>body</h4>
    <%= f.text_area :body %>
  </div>
  <%= f.submit %>
<% end %>